#include <iostream>

const int N = 100010;

// int a[N], s[N];

using namespace std;

// 双指针算法

int main()
{
    // 最长连续不重复子串
    // int n, ans = 0;
    // cin >> n;
    // for (int i = 0; i < n; i++)
    //     cin >> a[i];
    // for (int i = 0, j = 0; i < n; i++)
    // {
    //     s[a[i]]++;
    //     while (j < i && s[a[i]] > 1)
    //     {
    //         s[a[j]]--;
    //         j++;
    //     }
    //     ans = max(ans, i - j + 1);
    // }
    // cout << ans << '\n';

    // 数组元素的目标和
    // int n, m, x;
    // cin >> n >> m >> x;
    // int a[n], b[m];
    // for (int i = 0; i < n; i++)
    //     cin >> a[i];
    // for (int i = 0; i < m; i++)
    //     cin >> b[i];
    // int i, j;
    // for (i = 0, j = m - 1; i < n; i++)
    // {
    //     while (j >= 0 && a[i] + b[j] > x)
    //         j--;
    //     if (a[i] + b[j] == x)
    //         break;
    // }
    // cout << i << ' ' << j << '\n';


    system("pause");
    return 0;
}
